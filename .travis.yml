language: node_js
node_js:
  - 12
env:
  - 'DATABASE_URL=postgresql://coder:pg123@localhost:5432/postgres'
before_script:
  - psql -c 'create databasepostgres' -U postgres  
  - psql -U postgres -d postgres -a -f sql/db.sql
 
services:
  - postgresql